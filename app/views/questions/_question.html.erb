<h4>Question</h4>
<%= question.body %>

<h5>Answer Choices</h5>

<% question.answers.each do |answer| %>
  <%= render partial: "answers/show", locals: { answer: answer } %>




    <%= link_to "New Question", new_survey_question_path(@survey, @question) %>
      <% end %>
      <% end %>
